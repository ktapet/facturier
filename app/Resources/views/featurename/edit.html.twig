{% extends 'product/product_layout.html.twig' %}

{% block body %}
    <h1>Feature Name Edit</h1>
    {{ form_start(edit_form, {'attr': {'class': 'form-horizontal'}}) }}
    
   <div class="form-group">
        {{ form_label(edit_form.fnValue, 'Feature name', {'label_attr': {'class': 'col-md-1 col-sm-2 control-label'}}) }}
       
        <div class="col-md-3 col-sm-4">
            {{ form_widget(edit_form.fnValue, {'attr': {'class': 'form-control'}}) }}
        </div>
         {{ form_errors(edit_form.fnValue) }}
    </div>    
    <div class="form-group">
        <div class="col-md-3 col-md-offset-1 col-sm-4 col-sm-offset-2">
            {{ form_widget(edit_form.submit, { 'label': 'Save', 'attr': {'class': 'btn btn-sm btn-success'}}) }}
        </div>
    </div>
    {{ form_end(edit_form) }}
    
    {{ form_start(delete_form, {'attr': {'class': 'form-horizontal'}}) }}
    <div class="form-group">

        <div class="col-md-3 col-md-offset-1 col-sm-4 col-sm-offset-2">
           {{ form_widget(delete_form.submit, { 'label': 'Delete', 'attr': {'class': 'btn btn-sm btn-danger'}}) }}
        </div>

    </div> 
    {{ form_end(delete_form) }}  
    <a href="{{ path('featurename_index') }}" class="btn btn-default"><span class="glyphicon glyphicon-arrow-left"></span> Back to the list</a>
{% endblock %}
